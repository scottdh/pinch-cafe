name: Trigger Build
on:
  push
jobs:
  build_project:
    runs-on: ubuntu-latest
    steps:
    - name: Webhook
      uses: distributhor/workflow-webhook
      with:
        url: "https://dev.sansay.co.uk/api/runTask?taskID=pinch-cafe"
